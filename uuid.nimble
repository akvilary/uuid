# Package
version       = "0.1.0"
author        = "akvilary"
description   = "RFC 9562 UUID library for Nim — stack-allocated, versions 1-8"
license       = "MIT"
srcDir        = "src"

# Dependencies
requires "nim >= 2.0.0"
requires "checksums >= 0.1.0"
